"""Tests for CSV export (logbook/export.py).

Stable full-width headers, display-name resolution without config.json present,
soft-deleted rows exported and flagged, and atomic replace.

Build order: step 4. Fixtures are generated here, never committed.
"""
