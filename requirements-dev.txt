# Dev-only. The RUNTIME path has zero third-party dependencies and must stay
# that way (stdlib + Tkinter only). The test suite itself uses the stdlib
# `unittest` module, so it needs nothing installed:
#     python -m unittest discover -s tests -t .
# pytest is optional — if installed it runs the same tests with nicer output.
pytest
