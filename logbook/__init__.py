"""Vessel Logbook — a stdlib-only Tkinter logbook for a small sailing yacht.

Replaces a paper notebook aboard, running alongside OpenCPN on a low-powered
Debian machine. Standard library only: no compiled dependencies, identical on
amd64 and arm64.

Authoritative design: docs/logbook-scope.md (Draft 4).
"""

__version__ = "0.0.0"  # pre-implementation skeleton
